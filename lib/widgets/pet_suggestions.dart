import 'package:flutter/material.dart';

class PetSuggestions extends StatelessWidget {
  const PetSuggestions({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('PetSuggestions'),
    );
  }
}
