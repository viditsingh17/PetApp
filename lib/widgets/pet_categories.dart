import 'package:flutter/material.dart';

class PetCategories extends StatelessWidget {
  const PetCategories({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('PetCategories'),
    );
  }
}
